A code wiki for this local repository. Prioritize a concise quickstart, architecture overview, source map, key workflows, domain concepts, operations/runbook notes, testing guidance, and integration points. Inspect git history to understand reasoning behind code changes and the progression of the repository. Keep pages grounded in the repository structure and recent code changes. Prefer practical navigation for engineers over generic summaries.

Describe Domnotate as client-first, not browser-only: local sessions use IndexedDB, URL loading makes a server-side request through the Pages Function proxy, and sharing uploads captured HTML, annotations, and edits to R2. State that shared links have no identity layer, anyone with the link can view and edit, and concurrent edits are last-write-wins. Warn users not to share confidential HTML, embedded credentials, private customer data, or other sensitive content. Treat the 5 MB limit as application-enforced, but describe retention, rate limiting, billing notifications, bindings, and runtime behavior as operator-configured unless repository code proves otherwise. Use placeholders for account-specific resource names in deployment examples, recommend narrowly scoped API tokens instead of the Global API Key, and keep secrets and local Cloudflare state in platform secret stores or ignored local files. Never include credentials, real account or zone IDs, personal deployment URLs, environment values, dashboard exports, or personal operational settings.
